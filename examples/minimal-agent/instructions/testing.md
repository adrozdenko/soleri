# Testing Rules

- Write tests before implementation (TDD)
- Every PR must have test coverage for changed code
- Integration tests must hit real databases, not mocks
- Run `npm test` before committing
