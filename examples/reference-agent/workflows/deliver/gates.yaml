gates:
  - phase: pre-execution
    requirement: All tests pass and code quality checks are green
    check: tests-pass

  - phase: post-task
    requirement: Test coverage meets threshold (>=80%) and no regressions
    check: coverage-threshold

  - phase: completion
    requirement: Release tagged and knowledge captured
    check: knowledge-captured
