---
import BaseLayout from '../../layouts/BaseLayout.astro';
import CTA from '../../components/CTA.astro';
import { t } from '../../i18n/ui';
import { locales } from '../../i18n/types';
import type { Locale } from '../../i18n/types';
import { howItWorksContent } from '../../i18n/pages/how-it-works';
import '../../styles/how-it-works.css';

export function getStaticPaths() {
  return locales.map(locale => ({ params: { locale }, props: { locale } }));
}

const { locale } = Astro.props as { locale: Locale };
const c = howItWorksContent(locale);
---
<BaseLayout
  title={c.title}
  description={c.description}
  lang={locale}
  locale={locale}
>
  <!-- PAGE HERO -->
  <section class="page-hero">
    <div class="soleri-mark mark-hero"></div>
    <p class="eyebrow">{c.eyebrow}</p>
    <h1>{c.heroTitle}</h1>
    <p class="hero-subtitle">{c.heroSubtitle}</p>
  </section>

  <!-- ARCHITECTURE DIAGRAM -->
  <div class="arch-diagram">
    {c.archLayers.map(layer => (
      <div class="arch-layer">
        <p class="arch-layer-name"><span class="arch-bar"></span> {layer.name}</p>
        <p class="arch-layer-desc" set:html={layer.desc} />
      </div>
    ))}
  </div>

  <div class="soleri-mark mark-section"></div>

  <!-- VAULT SECTION -->
  <section class="deep-section">
    <div class="deep-grid">
      <div class="deep-content">
        <h3>{c.vault.title}</h3>
        <p>{c.vault.text}</p>
        <p class="key-point">{c.vault.keyPoint}</p>
      </div>
      <div>
        <div class="feature-code" set:html={c.vault.code1} />
        <div class="feature-code" style="margin-top:12px;" set:html={c.vault.code2} />
      </div>
    </div>
  </section>

  <!-- BRAIN SECTION -->
  <section class="deep-section">
    <div class="deep-grid">
      <div>
        <div class="feature-code" set:html={c.brain.code} />
        <div class="bar-chart">
          <div class="bar-row">
            <span class="bar-label">semantic-tokens</span>
            <div class="bar-track"><div class="bar-fill amber" style="width:94%"></div></div>
            <span class="bar-value">94</span>
          </div>
          <div class="bar-row">
            <span class="bar-label">components</span>
            <div class="bar-track"><div class="bar-fill teal" style="width:87%"></div></div>
            <span class="bar-value">87</span>
          </div>
          <div class="bar-row">
            <span class="bar-label">error-boundaries</span>
            <div class="bar-track"><div class="bar-fill green" style="width:82%"></div></div>
            <span class="bar-value">82</span>
          </div>
          <div class="bar-row">
            <span class="bar-label">a11y-focus</span>
            <div class="bar-track"><div class="bar-fill teal" style="width:78%"></div></div>
            <span class="bar-value">78</span>
          </div>
          <div class="bar-row">
            <span class="bar-label">zustand</span>
            <div class="bar-track"><div class="bar-fill amber" style="width:71%"></div></div>
            <span class="bar-value">71</span>
          </div>
        </div>
      </div>
      <div class="deep-content">
        <h3>{c.brain.title}</h3>
        <p>{c.brain.text}</p>
        <p class="key-point">{c.brain.keyPoint}</p>
      </div>
    </div>
  </section>

  <!-- KNOWLEDGE LIFECYCLE SECTION -->
  <section class="deep-section">
    <div class="deep-grid">
      <div class="deep-content">
        <h3>{c.lifecycle.title}</h3>
        <p>{c.lifecycle.text}</p>
        <div class="lifecycle-flow">
          {c.lifecycle.steps.map((step, i) => (
            <>
              {i > 0 && <div class="lifecycle-arrow">&darr;</div>}
              <div class="lifecycle-step">
                <span class={`lifecycle-step-num ${step.color}`}>{i + 1}</span>
                <div class="lifecycle-step-text">
                  <strong>{step.label}</strong>
                  <span class="lifecycle-step-desc">{step.desc}</span>
                </div>
              </div>
            </>
          ))}
        </div>
        <p class="key-point">{c.lifecycle.keyPoint}</p>
      </div>
      <div>
        <div class="feature-code" set:html={c.lifecycle.code1} />
        <div class="feature-code" style="margin-top:12px;" set:html={c.lifecycle.code2} />
      </div>
    </div>
  </section>

  <div class="soleri-mark mark-section"></div>

  <!-- TRANSPORT SECTION -->
  <section class="deep-section">
    <div class="deep-grid">
      <div class="deep-content">
        <h3>{c.transport.title}</h3>
        <p>{c.transport.text}</p>
        <p class="key-point">{c.transport.keyPoint}</p>
      </div>
      <div>
        <div class="feature-code" set:html={c.transport.code} />
      </div>
    </div>
  </section>

  <!-- CTA -->
  <CTA locale={locale} />
</BaseLayout>
