# Temporary until the @soleri/domain-* community packs widen their
# peerDependencies to "@soleri/core": "^9.0.0 || ^10.0.0". They are
# devDependencies here and run fine against core v10; without this flag
# npm ci refuses to install past the stale ^9.0.0 peer range.
legacy-peer-deps=true
